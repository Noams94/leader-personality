# מפת הפרויקט — Leader Personality

## 📝 טקסט — ערכו כאן בלבד

| קובץ | תוכן |
|------|------|
| `messages/he.json` | כל הטקסט בעברית |
| `messages/en.json` | כל הטקסט באנגלית |

### מבנה ה-JSON:
| מקטע | מה הוא מכסה |
|------|------------|
| `"nav"` | תפריט ניווט (בית, מחקר, אודות, שאלון) |
| `"home"` | דף הבית — כותרת, תיאור, כפתורים, וידאו, CTA |
| `"factors"` | שם + אימוג׳י + תיאור קצר לכל אחד מ-5 הגורמים |
| `"research"` | דף המחקר — כל הטקסטים, תיאורי מחקר 2 ו-4, הסברי הגורמים |
| `"about"` | דף אודות — ביו, ממצאים, ציטוט, יצירת קשר |
| `"assessment"` | השאלון — כרטיסי בחירת תפקיד, הוראות, כפתורים |
| `"results"` | דף התוצאות — כותרות, ציונים, פרשנות, כפתורי PDF |

**כללי עריכה:**
- שנו רק את הערכים מימין לנקודתיים (`:`)
- אל תשנו את שמות המפתחות (כמו `"heroTitle"`)
- גרשיים בתוך טקסט חייבים להיות מוחמקים: `\"`
- שני הקבצים חייבים לכלול את **אותם מפתחות**

---

## 🎨 עיצוב — צבעים, גופנים, spacing

| קובץ | תוכן |
|------|------|
| `app/globals.css` | CSS גלובלי — גופנים, reset, צבעי בסיס |

> Tailwind v4 מוגדר ישירות ב-`globals.css` (אין `tailwind.config` נפרד).
> כל `className="bg-blue-600 rounded-xl..."` בקבצי `.tsx` הוא גם עיצוב — Tailwind inline.

---

## 🏗️ מבנה דפים — מה מוצג בכל עמוד

| קובץ | עמוד |
|------|------|
| `app/[locale]/page.tsx` | **דף הבית** — Hero, וידאו, גורמים, CTA |
| `app/[locale]/research/page.tsx` | **דף המחקר** |
| `app/[locale]/about/page.tsx` | **דף אודות** |
| `app/[locale]/assessment/page.tsx` | **עמוד השאלון** (wrapper דק) |
| `app/[locale]/results/page.tsx` | **עמוד התוצאות** |
| `app/[locale]/layout.tsx` | Layout לכל העמודים — NavBar + כיוון RTL/LTR |
| `app/layout.tsx` | Root layout — תגי `<html>` |

---

## 🧩 רכיבים — אבני בניין שמשמשים מספר עמודים

| קובץ | תפקיד |
|------|--------|
| `components/NavBar.tsx` | סרגל ניווט עליון + מתג שפה |
| `components/AssessmentWizard.tsx` | כל לוגיקת השאלון — בחירת תפקיד, שלבים, כפתורים |
| `components/LikertItem.tsx` | כפתורי הדירוג 1–5 לכל תואר |
| `components/ResultsRadar.tsx` | גרף הרדאר (recharts) |
| `components/AssessmentPDF.tsx` | תבנית ה-PDF להורדה |

---

## 🔢 לוגיקה ונתונים — חישובים ומידע מחקרי

| קובץ | תוכן |
|------|------|
| `lib/items.ts` | **70 פריטי השאלון** — עברית, אנגלית, גורם, reversed |
| `lib/benchmarks.ts` | נקודות ייחוס מהמחקר לפי מגזר (עסקי / צבאי / דתי) |
| `lib/scoring.ts` | פונקציות חישוב הציונים (מנהל 5 גורמים / עובד 7 גורמים) |
| `lib/types.ts` | הגדרות TypeScript — טיפוסי נתונים |

---

## ⚙️ תצורה — אל תיגעו אלא אם יש סיבה טובה

| קובץ | תוכן |
|------|------|
| `next.config.ts` | הגדרות Next.js + next-intl |
| `i18n/routing.ts` | הגדרת locale: `he` (ברירת מחדל) + `en` |
| `i18n/request.ts` | טעינת קבצי תרגום לפי locale |
| `i18n/navigation.ts` | Link ו-redirect מודעי-locale |
| `tsconfig.json` | הגדרות TypeScript |
| `postcss.config.mjs` | PostCSS עם Tailwind |
| `package.json` | תלויות הפרויקט |

---

## 🖼️ תמונות ואייקונים

| קובץ | תוכן |
|------|------|
| `app/favicon.ico` | האייקון בטאב הדפדפן |
| `public/` | תמונות SVG (לא בשימוש כרגע) |

---

## 🗺️ סיכום מהיר

| רוצים לשנות... | עברו אל... |
|---------------|-----------|
| טקסט | `messages/he.json` או `messages/en.json` |
| צבעים / עיצוב | `app/globals.css` + className בקובץ הרלוונטי |
| תוכן / מבנה עמוד | `app/[locale]/[שם-עמוד]/page.tsx` |
| פריטי שאלון | `lib/items.ts` |
| נקודות ייחוס | `lib/benchmarks.ts` |
